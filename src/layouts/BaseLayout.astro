---
import '../styles/global.css';
import SEOHead from '../components/SEOHead.astro';
import JsonLd from '../components/JsonLd.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
  image?: string;
  imageAlt?: string;
  type?: 'website' | 'article';
  publishedDate?: Date;
  modifiedDate?: Date;
  noindex?: boolean;
}

const {
  title,
  description = '2030 직장인을 위한 실용 재테크 가이드. 저축, 투자, 세금, 절약 팁까지 알기 쉽게 정리합니다.',
  image = '/og-default.png',
  imageAlt,
  type = 'website',
  publishedDate,
  modifiedDate,
  noindex = false,
} = Astro.props;
---

<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href="/favicon-32.png" />
    <link rel="icon" href="/favicon.ico" sizes="32x32" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <meta name="generator" content={Astro.generator} />

    <!-- Pretendard Font -->
    <link rel="stylesheet" as="style" crossorigin href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css" />

    <!-- SEO 메타 태그 -->
    <SEOHead
      title={title}
      description={description}
      image={image}
      imageAlt={imageAlt}
      type={type}
      publishedDate={publishedDate}
      modifiedDate={modifiedDate}
      noindex={noindex}
    />

    <!-- JSON-LD: 사이트 정보 -->
    <JsonLd type="WebSite" />

    <!-- GA4 측정 ID를 실제 ID로 교체하세요 -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-NCE5SR12EC"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-NCE5SR12EC');
    </script>

    <!-- Google AdSense -->
    <meta name="google-adsense-account" content="ca-pub-2355375334646881" />
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-2355375334646881" crossorigin="anonymous"></script>
  </head>
  <body class="min-h-screen flex flex-col bg-neutral-50 text-neutral-800 font-sans">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>
